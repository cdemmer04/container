apiVersion: apps/v1
kind: Deployment
metadata:
  name: first-deployment-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-container
  template:
    metadata:
      labels:
        app: my-container
        env: test
    spec:
      containers:
      - name: my-container
        image: europe-west1-docker.pkg.dev/opdrachten-486314/pcc-week3/app:test
        imagePullPolicy: Always
        ports:
        - containerPort: 80


