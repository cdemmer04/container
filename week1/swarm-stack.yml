version: "3.8"

services:

  eshopwebmvc:
    image: ${DOCKER_REGISTRY}/chieldemmer:latest
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == worker
    ports:
      - target: 80       
        published: 80    
        protocol: tcp
        mode: host